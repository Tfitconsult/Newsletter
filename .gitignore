# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
**/.env
.env
.env.local
.env.*.local
*.log

# Node / MJML / Mermaid
node_modules/
.mermaid-cache/
npm-debug.log*
# If you add a package-lock.json, you may want to commit it; we ignore here by default
# package-lock.json

# Build / Artifacts
build/
dist/
plantuml.jar

# PDFs generated locally (CI commits to docs/site instead)
*.pdf

# OS / IDE
.DS_Store
.idea/
.vscode/

# Docs site artifacts (CI can commit selectively)
docs/site/
